name: Shipkie Staging Deployment

on:
  push:
    branches: [master]

jobs:
  deploy:
    needs: test_pull_request
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: ${{secrets.HEROKU_API_KEY}}
          heroku_app_name: 'darkel-ig-profile' #Must be unique in Heroku
          heroku_email: 'kelvinjune2@gmail.com'
